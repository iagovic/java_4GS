package com.example.SkillBridge.repository;

import com.example.SkillBridge.model.AppAuditoria;
import org.springframework.data.jpa.repository.JpaRepository;
public interface AppAuditoriaRepository extends JpaRepository<AppAuditoria, Long> {
}